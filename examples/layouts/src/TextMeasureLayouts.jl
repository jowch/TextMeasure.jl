module TextMeasureLayouts

# Shared layout utilities for the TextMeasure.jl demos milestone.
#
# This package is the milestone home of `shape_pack` (#C) and (stretch) `knuth_plass`
# (#K). It is consumed by the per-demo projects via `Pkg.develop(path="../layouts")`.
# Long-term migration target: a registered `TextMeasureLayouts.jl` sibling package.
#
# Implementation lands during the approved plan; this is the package skeleton used to
# instantiate the env at workspace-setup time.

end # module
